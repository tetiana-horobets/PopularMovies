<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@+id/settings"
        android:title="@string/action_settings"
        app:showAsAction="always"
        android:visible="true">

        <group android:id="@+id/action_settings" android:checkableBehavior="single">

            <item android:id="@+id/menu_popular"
                android:title="@string/menu_popular_title"
                android:checked="true" />

            <item android:id="@+id/menu_top"
                android:title="@string/menu_top_title" />

            <item android:id="@+id/menu_favorite"
                android:title="@string/menu_favorite_title"
                />

        </group>

    </item>
</menu>